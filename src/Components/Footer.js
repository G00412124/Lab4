const Footer = () => { //this is the Footer component that returns the JSX code
    return <h3>My Footer in another component</h3>; //this is the return statement that returns the JSX code
  };
  
  export default Footer; //this exports the Footer component so it can be imported in other files