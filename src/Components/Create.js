const Create = () => { //this is the Create component that returns the JSX code
    return (
        <div>
        <h2>Create</h2> {/*//this is the title of the Create component*/}
        </div>
    );
    }

export default Create; //this exports the Create component so it can be imported in other files