  
const Header = () => { //this is the Header component that returns the JSX code
    return <h1 className="text-center blue-header">My Header in another component</h1>; //this is the return statement that returns the JSX code
};

export default Header; //this exports the Header component so it can be imported in other files